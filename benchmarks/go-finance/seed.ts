/**
 * Go Finance benchmark seeder.
 *
 * Reuses the standard finance benchmark seeder against any host:
 *   deno run --allow-net benchmarks/go-finance/seed.ts --host=http://localhost:1985
 */

import "../finance/seed.ts";

